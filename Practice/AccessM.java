class Access_Modifier_example{
    //default - accessible in own package and not outside package
    //private - only inside class
    //public - everywhere
    //protected - only classes extending the class in own or outside package

    //has a - relationship - means the subclass is using the main class object means it is creating the object of main class
    //is s - relationship - means the subclass is extending from the main class 
}